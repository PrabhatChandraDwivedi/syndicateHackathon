# ReconcileOS — Python 3.11–3.13 (Neatlogs requires >=3.10,<3.14)
# Install:  uv venv && uv pip install -r requirements.txt

# --- API server
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12        # file upload on POST /ingest/{source}
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- data + matching
pandas>=2.2
rapidfuzz>=3.10                 # merchant / entity fuzzy resolution
python-dateutil>=2.9

# --- scheduling (H5 continuous close)
apscheduler>=3.10

# --- inference (TensorMux primary, OpenAI fallback — both OpenAI-compatible)
openai>=1.54
httpx>=0.27

# --- observability (H9)
neatlogs>=0.1

# --- payments (H: Dodo webhooks). Verification is stdlib hmac; SDK optional.
# dodopayments>=1.0

# --- config
pyyaml>=6.0                     # policies.yaml, models.yaml, adapters/*.yaml

# --- tests
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27                     # FastAPI TestClient transport
freezegun>=1.5                  # deterministic timestamps in audit tests
